---
import { cn } from "../utils";

type Props = {
  class?: string;
  id?: string;
};

const { class: className, id } = Astro.props;
---

<section
  class={cn(
    "mx-auto my-20 flex max-w-6xl flex-col px-6 md:my-26 md:px-12",
    className,
  )}
  id={id}
>
  <slot />
</section>
