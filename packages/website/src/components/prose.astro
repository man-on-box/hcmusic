---
import { cn } from "../utils";

interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div
  class={cn(
    "prose md:prose-h2:text-5xl prose-h2:text-4xl prose-a:text-blue-600 prose-img:rounded-xs px-6",
    className,
  )}
>
  <slot />
</div>
